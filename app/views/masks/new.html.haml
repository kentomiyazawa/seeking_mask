.mask-form
  - if @mask.errors.any?
    .mask-form__errors
      %ul
        - @mask.errors.full_messages.each do |message|
          %li
            = message
  %h1
    新規在庫入力画面
  = render partial: "form"

    